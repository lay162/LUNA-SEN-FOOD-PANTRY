/**
 * Primary operational inbox (Namecheap Private Email).
 * Route aliases in the mailbox later: support@ / referrals@ can forward to this address or replace these exports.
 */
export const ADMIN_EMAIL = 'admin@lunasenpantry.co.uk';

/** General enquiries and support — same inbox until support@ is live */
export const SUPPORT_EMAIL = ADMIN_EMAIL;

/** Referral notifications — same inbox until referrals@ is live */
export const REFERRALS_EMAIL = ADMIN_EMAIL;

export const EMERGENCY_PHONE_TEL = '07718851362';
export const EMERGENCY_PHONE_DISPLAY = '07718 851362';
